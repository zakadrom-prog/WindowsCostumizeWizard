<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- Window Info -->
    <SolidColorBrush x:Key="WI_BorderBackground" Color="Black" Opacity="0.8"/>
    <SolidColorBrush x:Key="MW_T" Color="Black" Opacity="0.5"/>
    <SolidColorBrush x:Key="WI_BorderBrush" Color="White"/>
    <BitmapImage x:Key="AppIcon" UriSource="/Resources/app_d.png"/>

    <!-- Global MainWindow Grid -->
    <SolidColorBrush x:Key="GridGlobalMWBg" Color="#222"/>

    <!-- Основний фон бордера TitleISO -->
    <SolidColorBrush x:Key="GlobalBorderBackground0" Color="#FF3E4744"/>
    <SolidColorBrush x:Key="GlobalBorderBackground1" Color="#FF1A1A1A"/>
    <SolidColorBrush x:Key="GlobalBorderBackground2" Color="#FF183248"/>
    <SolidColorBrush x:Key="GlobalBorderBackground3" Color="#222"/>
    <SolidColorBrush x:Key="GlobalBorderBackground4" Color="#FF2B3337"/>
    <SolidColorBrush x:Key="GlobalBorderBackground5" Color="#222"/>
    <SolidColorBrush x:Key="GlobalBorderBackground6" Color="#FF3E4744"/>
    <SolidColorBrush x:Key="GlobalConsoleColor" Color="#FF3E4744"/>

    <!-- Градієнт бордера -->
    <LinearGradientBrush x:Key="MainBorderGradient" StartPoint="0.5,0" EndPoint="0.5,1">
        <GradientStop Color="Black" Offset="0"/>
        <GradientStop Color="#FF1E124E" Offset="1"/>
    </LinearGradientBrush>

    <SolidColorBrush x:Key="GlobalBorderBrush00" Color="White"/>
    <SolidColorBrush x:Key="GlobalBorderBrush0" Color="DimGray"/>
    <SolidColorBrush x:Key="GlobalBorderBrush1" Color="Gray"/>
    <SolidColorBrush x:Key="GlobalBorderBrush3" Color="#FF5F9E5F"/>

    <SolidColorBrush x:Key="GlobalTitleForeground0" Color="Gold"/>
    <SolidColorBrush x:Key="GlobalTitleForeground1" Color="#FF9FCBF2"/>
    <SolidColorBrush x:Key="GlobalTitleForeground2" Color="Cyan"/>
    <SolidColorBrush x:Key="GlobalTitleForeground3" Color="#FF5F9E5F"/>
    <SolidColorBrush x:Key="GlobalTitleForeground4" Color="Pink"/>
    <SolidColorBrush x:Key="GlobalTitleForeground5" Color="#FFAC7DEA"/>
    <SolidColorBrush x:Key="Separator" Color="DarkCyan"/>
    <SolidColorBrush x:Key="ColorProgres" Color="YellowGreen"/>

    <SolidColorBrush x:Key="JurnalBackground0" Color="#222"/>
    <SolidColorBrush x:Key="JurnalBackground1" Color="#FF292929"/>

    <!-- Кнопки -->
    <Style x:Key="MButtonD" TargetType="Button">

        <Setter Property="Height" Value="25"/>
        <Setter Property="MinWidth" Value="50"/>
        <Setter Property="Foreground" Value="LightSeaGreen"/>
        <Setter Property="BorderBrush" Value="DimGray"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="FontWeight" Value="Medium"/>
        <Setter Property="Cursor" Value="Hand"/>

        <Setter Property="Background">
            <Setter.Value>
                <LinearGradientBrush StartPoint="0.5,0" EndPoint="0.5,1">
                    <GradientStop Color="Black"/>
                    <GradientStop Color="#FF053E4E" Offset="1"/>
                </LinearGradientBrush>
            </Setter.Value>
        </Setter>

        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="border"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}">
                        <ContentPresenter
                                    HorizontalAlignment="Center"
                                    VerticalAlignment="Center"/>
                    </Border>

                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="border"
                                    Property="Background">
                                <Setter.Value>
                                    <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                                        <GradientStop Color="#FF3A3838"/>
                                        <GradientStop Color="#FF5D5D5D" Offset="1"/>
                                    </LinearGradientBrush>
                                </Setter.Value>
                            </Setter>
                        </Trigger>

                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="border" Property="RenderTransform">
                                <Setter.Value>
                                    <TranslateTransform Y="1"/>
                                </Setter.Value>
                            </Setter>
                        </Trigger>

                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="border"
                                    Property="Opacity"
                                    Value="0.5"/>
                        </Trigger>
                    </ControlTemplate.Triggers>

                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <Style x:Key="GlobalActionButtonStyle" TargetType="Button" BasedOn="{StaticResource MButtonD}"/>
    <!-- End -->

    <!-- Кнопки -->
    <Style x:Key="MButtonDC" TargetType="Button">

        <Setter Property="Height" Value="20"/>
        <Setter Property="MinWidth" Value="20"/>
        <Setter Property="Foreground" Value="LightSeaGreen"/>
        <Setter Property="BorderBrush" Value="DimGray"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="FontWeight" Value="Medium"/>
        <Setter Property="Cursor" Value="Hand"/>

        <Setter Property="Background">
            <Setter.Value>
                <LinearGradientBrush StartPoint="0.5,0" EndPoint="0.5,1">
                    <GradientStop Color="Black"/>
                    <GradientStop Color="#FF053E4E" Offset="1"/>
                </LinearGradientBrush>
            </Setter.Value>
        </Setter>

        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="border"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}">
                        <ContentPresenter
                                    HorizontalAlignment="Center"
                                    VerticalAlignment="Center"/>
                    </Border>

                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="border"
                                    Property="Background">
                                <Setter.Value>
                                    <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                                        <GradientStop Color="#FF3A3838"/>
                                        <GradientStop Color="#FF5D5D5D" Offset="1"/>
                                    </LinearGradientBrush>
                                </Setter.Value>
                            </Setter>
                        </Trigger>

                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="border" Property="RenderTransform">
                                <Setter.Value>
                                    <TranslateTransform Y="1"/>
                                </Setter.Value>
                            </Setter>
                        </Trigger>

                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="border"
                                            Property="Opacity"
                                            Value="0.5"/>
                        </Trigger>
                    </ControlTemplate.Triggers>

                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <Style x:Key="GlobalActionButtonStyleC" TargetType="Button" BasedOn="{StaticResource MButtonDC}"/>
    <!-- End -->
</ResourceDictionary>

    