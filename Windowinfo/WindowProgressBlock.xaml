<Window x:Class="WindowsCostumizeWizard.WindowInfo.WindowProgressBlock"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WindowsCostumizeWizard.WindowInfo"
        mc:Ignorable="d" WindowStyle="None" AllowsTransparency="True"
        Background="Transparent" ResizeMode="NoResize" ShowInTaskbar="False"
        SizeToContent="WidthAndHeight" WindowStartupLocation="CenterOwner">

    <Border Background="{DynamicResource WI_BorderBackground}"
            BorderBrush="{DynamicResource WI_BorderBrush}"
            BorderThickness="1" Padding="20">

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Grid Width="64" Height="25" Margin="0,0,0,10">
                <StackPanel Orientation="Horizontal"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center">

                    <Ellipse x:Name="dot1" Width="8" Height="8" Fill="DodgerBlue" Margin="3"/>
                    <Ellipse x:Name="dot2" Width="8" Height="8" Fill="DodgerBlue" Margin="3"/>
                    <Ellipse x:Name="dot3" Width="8" Height="8" Fill="DodgerBlue" Margin="3"/>
                </StackPanel>

                <Grid.Triggers>
                    <EventTrigger RoutedEvent="Loaded">
                        <BeginStoryboard>
                            <Storyboard RepeatBehavior="Forever">

                                <!-- 1 -->
                                <DoubleAnimation Storyboard.TargetName="dot1"
                                     Storyboard.TargetProperty="Opacity"
                                     From="0.2" To="1"
                                     Duration="0:0:0.5"
                                     AutoReverse="True"/>

                                <!-- 2 -->
                                <DoubleAnimation Storyboard.TargetName="dot2"
                                     Storyboard.TargetProperty="Opacity"
                                     From="0.2" To="1"
                                     Duration="0:0:0.5"
                                     BeginTime="0:0:0.2"
                                     AutoReverse="True"/>

                                <!-- 3 -->
                                <DoubleAnimation Storyboard.TargetName="dot3"
                                     Storyboard.TargetProperty="Opacity"
                                     From="0.2" To="1"
                                     Duration="0:0:0.5"
                                     BeginTime="0:0:0.4"
                                     AutoReverse="True"/>

                            </Storyboard>
                        </BeginStoryboard>
                    </EventTrigger>
                </Grid.Triggers>
            </Grid>

            <TextBlock Text="{Binding}" FontSize="15" FontStyle="Italic"
                       Foreground="{DynamicResource GlobalTitleForeground2}"
                       VerticalAlignment="Center" Grid.Row="1"
                       HorizontalAlignment="Center" FontWeight="Bold"/>
        </Grid>
    </Border>
</Window>
